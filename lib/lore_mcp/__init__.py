"""lore_mcp — MCP server exposing vault retrieval to any MCP client.

Phase B scope. Wraps `lore_search` and `lore_core.lint` outputs with
the MCP tool surface: lore_search, lore_read, lore_index, lore_catalog,
lore_resume, lore_wikilinks.

Not implemented in v0.1.
"""
