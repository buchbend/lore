__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
dist/
build/
*.egg
.coverage
htmlcov/

# Local config (per-user, not for the plugin repo itself)
.env
.env.local

# Search index caches — regenerated from source
.cache/

# Claude Code local state
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Git worktrees (local workspaces, never committed)
.worktrees/

# Personal session-handover notes (per-user)
NEXT_SESSION.md
